{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "web 包 — 前端应用，依赖 shared",

  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "jsx": "react-jsx"
  },
  "references": [
    { "path": "../shared" }
  ],
  "include": ["src/**/*"]
}
